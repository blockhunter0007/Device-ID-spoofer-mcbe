name: Build and Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if (Test-Path requirements.txt) {
            pip install -r requirements.txt
          }
          pip install pyinstaller

      - name: Build EXE
        run: |
          pyinstaller --noconsole --onefile --name "Divice ID spoofer-onefile" "divice_id_spoofer.py"

      - name: Build Standalone EXE
        run: |
          pyinstaller --noconsole --onedir --name "Divice ID spoofer-standalone" "divice_id_spoofer.py"
      
      - name: Zip Standalone Build
        run: |
          Compress-Archive `
            -Path "dist/Divice ID spoofer-standalone/*" `
            -DestinationPath "dist/Divice ID spoofer-standalone.zip"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "Divice ID Spoofer ${{ github.ref_name }}"
          body_path: "release-notes/${{ github.ref_name }}.md"
          files: |
            dist/Divice ID spoofer-onefile.exe
            dist/Divice ID spoofer-standalone.zip